const { default: axios } = require("axios");

function curDate(req,res) {
    // axios.get('https://www.timeapi.io/api/Time/current/zone?timeZone=Asia/Bangkok')
    // .then(response => {
    //     req.currentTime = response.data
    // })
    // .catch(err => {
    //     console.log(err)
    // })
    // req.curDate = await
}

module.exports={curDate}